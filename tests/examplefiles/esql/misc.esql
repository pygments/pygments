// This is a comment
ROW test_keyword = 1
/*
/* Misc. request, might be not functional but grammatically
correct, in order to test multiple token combinations.*/
FROM logs-*
/*
*/
| STATS COUNT(`1.field`)
| EVAL my_count = `COUNT(``1.field``)`
| WHERE name == """Indiana "Indy" Jones""" OR /* ok */ name == "My \"Special\" Guest"
| WHERE date > 4d AND NOT date > 34  years
| WHERE value > 1.2e-3
